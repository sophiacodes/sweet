const assetReducer = (state = {}, action) => {
    let newState = state;
    switch (action.type) {
    //   case 'APPROVE': 
    //     newState = { ...state, ...action.payload };
    //     break;
      default: 
        break;
    }
    return newState;
}

export default assetReducer;